# CareerIQ Pro — Development & Test Dependencies
# Install with: pip install -r requirements-dev.txt
# (In addition to requirements.txt)

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0
pytest-mock==3.14.0
httpx==0.27.0           # For TestClient / async HTTP testing

# ─── Code Quality ────────────────────────────────────────────────────────────
flake8==7.0.0
black==24.4.2           # Auto code formatter: black .
isort==5.13.2           # Import sorter: isort .
mypy==1.9.0             # Type checker: mypy .

# ─── Development Utilities ───────────────────────────────────────────────────
pre-commit==3.7.0       # Git hooks for formatting before each commit
watchdog==4.0.0         # File watcher (used by uvicorn --reload)
ipython==8.22.2         # Better Python REPL for interactive debugging

# ─── ML Training (optional - install separately) ─────────────────────────────
# pip install pandas xgboost lightgbm joblib sentence-transformers spacy
# python -m spacy download en_core_web_sm
